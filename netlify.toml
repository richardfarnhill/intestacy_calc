[build]
  command = "npm run build"
  publish = "dist"

[context.production]
  publish = "dist"
  [[redirects]]
    from = "/"
    to = "/examples/index.html"
    status = 200

[context.deploy-preview]
  publish = "dist"